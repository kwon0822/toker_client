<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.toker">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".tcp.Application_Socket"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <activity android:name=".page.Page_Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".page.Page_Login" />
        <activity android:name=".page.Page_Signup" />
        <activity android:name=".page.Page_Find" />
        <activity android:name=".page.Page_Main" />
        <activity android:name=".page.Page_Level" />
        <activity android:name=".page.Page_Myinfo" />
        <activity android:name=".page.Page_Chat" />
        <activity android:name=".popup.Popup_Chat"
            android:theme="@style/Theme.AppCompat.Light.Dialog"/>
    </application>

</manifest>